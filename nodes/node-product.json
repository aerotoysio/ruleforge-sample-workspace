{
  "id": "node-product",
  "name": "Product (output shape)",
  "description": "Emits a saved Asset (product) — pick one from your library. To change specific values (e.g. Price), chain a Set or calc node downstream so the adjustment is visible in the workflow.",
  "category": "product",
  "ports": {
    "inputs": [],
    "params": [
      {
        "name": "value",
        "type": "any",
        "required": true,
        "description": "Pick a saved Asset (product) to emit. To adjust specific values (e.g. Price), chain a Set or calc node downstream.",
        "bindingKinds": ["asset"]
      }
    ],
    "outputs": [
      { "name": "out", "branch": "default" }
    ]
  },
  "ui": { "badge": "PROD", "icon": "package", "accent": "#3a3a3a" },
  "tags": ["output", "product", "shape"],
  "updatedAt": "2026-05-09T00:00:00.000Z"
}
