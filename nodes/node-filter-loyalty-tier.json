{
  "id": "node-filter-loyalty-tier",
  "name": "Filter — loyalty tier",
  "description": "Branches pass / fail. Pass when the passenger's loyalty tier (Gold, Silver, Platinum, …) matches one of the selected tiers. Source field is restricted to fields whose name contains 'tier', 'loyalty', or 'level'.",
  "category": "filter",
  "ports": {
    "inputs": [
      {
        "name": "source",
        "type": "string",
        "required": true,
        "description": "The loyalty-tier field — pick a string field from your request whose name contains 'tier', 'loyalty', or 'level'.",
        "hint": { "shape": "scalar", "namePattern": "(tier|loyalty|level|status|membership)", "schemaTypes": ["string"] },
        "bindingKinds": ["path", "context"]
      }
    ],
    "params": [
      {
        "name": "literal",
        "type": "string-array",
        "required": true,
        "description": "Which tiers should pass — type your own list, or pick from a loyalty reference table if your workspace has one.",
        "bindingKinds": ["literal", "ref-select"]
      },
      {
        "name": "arraySelector",
        "type": "string",
        "description": "When the source resolves to multiple passengers, which match decides pass/fail.",
        "enum": [
          { "value": "any",   "label": "Any match",   "description": "Pass if at least one pax is in the tier list." },
          { "value": "all",   "label": "All match",   "description": "Pass only if every pax is in the tier list." },
          { "value": "none",  "label": "None match",  "description": "Pass only when no pax is in the tier list." },
          { "value": "first", "label": "First only",  "description": "Test the first pax only." },
          { "value": "only",  "label": "Only one",    "description": "Pass only when there's exactly one pax AND it satisfies the test." }
        ]
      },
      {
        "name": "onMissing",
        "type": "string",
        "description": "What to do when the passenger has no tier set (e.g. non-loyalty members).",
        "enum": [
          { "value": "fail", "label": "Fail",  "description": "Treat as not matching." },
          { "value": "skip", "label": "Skip",  "description": "Stop and move on." },
          { "value": "pass", "label": "Pass",  "description": "Treat as matching." }
        ]
      }
    ],
    "outputs": [
      { "name": "pass", "branch": "pass" },
      { "name": "fail", "branch": "fail" }
    ]
  },
  "defaults": { "operator": "in", "arraySelector": "any", "onMissing": "skip", "caseInsensitive": true, "trim": true },
  "ui": { "badge": "TIER", "icon": "award", "accent": "#5b3a72" },
  "tags": ["filter", "loyalty", "passenger", "business-friendly"],
  "updatedAt": "2026-05-07T00:00:00.000Z"
}
